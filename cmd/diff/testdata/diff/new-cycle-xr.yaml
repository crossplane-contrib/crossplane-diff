# A new XR whose composition starts the XCycleA -> XCycleB -> XCycleA cycle.
apiVersion: ns.cycle.example.org/v1alpha1
kind: XCycleA
metadata:
  name: test-cycle
  namespace: default
spec:
  cycleField: "cycle-value"
