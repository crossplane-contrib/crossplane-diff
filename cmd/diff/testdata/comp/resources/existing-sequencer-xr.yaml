apiVersion: ns.diff.example.org/v1alpha1
kind: XNopResource
metadata:
  name: sequencer-test
  namespace: default
spec:
  coolField: existing-value
  crossplane:
    compositionRef:
      name: sequencer-composition
